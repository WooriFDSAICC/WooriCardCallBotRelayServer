global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: [alertname, application]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: default
    # 운영 시 Slack/PagerDuty webhook 으로 교체
    webhook_configs: []
